services:
  web:
    ports: !override
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/nginx.production.conf:/etc/nginx/conf.d/default.conf:ro
      - ${TLS_DIRECTORY:?Set TLS_DIRECTORY with fullchain.pem and privkey.pem}:/etc/nginx/tls:ro
