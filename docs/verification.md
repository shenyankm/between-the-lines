# 验证记录

验证时间：2026-09-13。本机 Miniconda Python 3.13.15；PostgreSQL 17.6 Docker 容器。

## 自动化结果

| 检查 | 结果 | 覆盖 |
|---|---|---|
| pytest | 21 passed | 剧情前置条件、角色权限、请求幂等、版本冲突、用户/角色/新存档隔离、OAuth 协议模拟、模型/工具预算、超时、供应商 429、失败后事实保留 |
| Ruff / ESLint | 通过 | 后端与前端静态检查 |
| Vitest | 2 passed | 中文 SSE、CRLF、多行数据和心跳解析 |
| Playwright | 桌面与手机 2 passed | 登录、完整通关、真实 Agent 图中的模拟采购工具、刷新恢复、生成结局回顾、无横向溢出与页面异常 |
| 前端生产构建 | 通过 | TypeScript 检查及 Vite 产物 |
| Docker 构建及运行 | 通过 | 独立测试项目中 Nginx、FastAPI、PostgreSQL、静态资源及代理 SSE |
| 生产 Nginx / Compose 配置 | 通过 | 测试证书下 nginx -t、生产覆盖文件解析；未配置真实域名证书 |
| 进程强制终止恢复 | 通过 | 工具提交后 kill 进程，重启保留采购事实与登录；无重复工作事件，无未完成对白 |
| 数据库恢复演练 | 通过 | pg_dump/pg_restore 到临时数据库，业务数据与 Agent 检查点均恢复，随后清理临时数据库 |

## 模拟压力测试

所有回合均通过 ChatDeepSeek 和 Deep Agents，HTTP 层模拟 DeepSeek 返回；每轮先调用采购工具，再生成对白。无真实模型费用。

| 并发回合 | 完成 | 失败率 | P50 | P95 | 模型调用次数 |
|---|---|---|---|---|---|
| 10 | 10 | 0% | 0.802 s | 0.811 s | 20 |
| 20 | 20 | 0% | 1.003 s | 1.026 s | 40 |
| 30 | 30 | 0% | 1.403 s | 1.447 s | 60 |

这组延迟仅证明本机服务链路在模拟负载下的表现，不代表 DeepSeek 实际延迟、账单或生产容量。原始报告位于 `artifacts/load-test.json`，重启测试位于 `artifacts/restart-test.json`，容器验证位于 `artifacts/container-smoke.json`。这些产物为本机验证输出，不包含模型密钥。

## 明确保留的外部依赖

- 按用户要求，当前只模拟 LLM 返回；真实 DeepSeek 联调未执行。
- 知乎 OAuth 使用合作方协议模拟测试，真实端点、字段及接入资格等待合作方资料。
- 已交付生产部署、TLS 和每日机外备份配置与脚本；实际国内服务器、域名证书、备份接收主机尚未配置，未声称已上线或已设置机外定时备份。
- 没有创建新的 Conda 环境；项目 `.venv` 和临时引导环境均已移除。
